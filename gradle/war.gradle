apply plugin: "war"

bootWar {
    mainClass = "ru.rgordeev.WebsocketloadtestApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
